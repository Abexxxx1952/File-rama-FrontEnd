import type { Metadata } from "next";
import { StatisticPage } from "@/srcApp/pages/statistic";

export const metadata: Metadata = {
  title: "Statistic",
  description: "Your statistic",
  icons: "/icons/logo.svg",
};

export default StatisticPage;
